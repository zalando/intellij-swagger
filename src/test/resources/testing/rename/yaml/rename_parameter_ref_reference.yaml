swagger: '2.0'
paths:
  /path:
    get:
      parameters:
        - $ref: '#/parameters/<caret>paramName'
parameters:
  paramName:
    name: limit
    in: query
    description: Limits the number of returned results
    required: false
    type: number
    format: int32
