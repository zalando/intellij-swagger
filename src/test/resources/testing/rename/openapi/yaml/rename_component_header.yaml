openapi: "3.0.0"
paths:
  /:
    get:
      responses:
        '200':
          content:
            application/json:
              examples:
                foo:
                  $ref: "#/components/examples/Header"
components:
  examples:
    <caret>Header:
