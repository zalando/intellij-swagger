openapi: 3.0.0
paths:
  /pathA:
    get:
      responses:
        200:
          headers:
            x-next:
              $ref: "#/components/headers/Example"
components:
  headers:
    <caret>Example:
