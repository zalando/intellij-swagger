openapi: "3.0.0"
paths:
  /:
    get:
      responses:
        '200':
          content:
            application/json:
              examples:
                foo:
                  $ref: "#/components/examples/Example"
components:
  examples:
    <caret>Example:
