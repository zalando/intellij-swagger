swagger: '2.0'
paths:
  /pets:
    get:
      responses:
        200:
          schema:
            $ref: <error descr="Can not resolve reference '#/definitions/Unknown'">'#/definitions/Unknown'</error>
definitions:
  Pet:
